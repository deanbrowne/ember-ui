/**
  First create the Ember.UI demo application namespace.  Then define all the content.  Finally call
  `Demo.initialize()` to start the app (from main.js).

  @namespace
*/
Demo = Em.Application.create();
